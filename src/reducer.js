import {
    CLEAR_CART,
    REMOVE,
    INCREASE,
    DECREASE,
    LOADING,
    DISPLAY_ITEMS,
} from "./actions";

// Reducer function
const reducer = (state, action) => {
    return state;
};

export default reducer;
